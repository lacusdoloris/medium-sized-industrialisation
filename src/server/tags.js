// oh yeah this just straight up doesn't fucking work lol! the tags are actually defined in
// the json.
/*const ITEM_TAG_MAPPING = {};*/

/** @param {Internal.TagEventJS} event */
export const setupItemTags = (_event) => {
    /*for (let [tagName, entries] of Object.entries(ITEM_TAG_MAPPING)) {
        for (let entry of entries) {
            if (!Item.exists(entry)) {
                console.error("fuck! no such item " + entry);
            } else {
                let wrapper = event.add(tagName, entry);
            }
        }
    }*/
};
